{% extends 'base.html.twig' %}

{% block title %}
    Edit Food
{% endblock %}

{% block body %}

<div>
    <h1 class = 'text-center'>
    Edit Food
    </h1>
    <div>
    {{form_start(form)}}
        {{form_widget(form)}}
        <button 
        type='submit'
        class = 'btn btn-success btn-sm'
        >
        Change
        </button>
    {{form_end(form)}}
    <a href="/food/show/{{food.id}}">Back</a>
    </div>
</div>

{% endblock %}